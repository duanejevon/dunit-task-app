# 2. ScaffoldApp

**Goal**: Initialize the project repo and build tooling for the chosen stack; get a blank window launching locally.
**Est.**: 1.5h
**Dependencies**: 01

## Steps
- [x] Init repo with package manager / project structure for chosen stack
- [x] Add dev script to launch the app in dev mode
- [x] Render a blank window/app shell
- [x] Add a build script (even if unpolished) to confirm a packaging path exists
- [x] Commit initial scaffold

## Definition of Done
- [x] Dev command launches a blank desktop window — `npm run dev` (verified via `npm run build` + code review; could not visually screenshot in this sandbox, see below)
- [x] Repo structure is ready for feature code

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Electron + React + TS scaffold built: `vite.config.ts`, `tsconfig*.json`, `src/main.tsx`/`App.tsx`/`index.css`, `electron/main.js`/`preload.js`. `npm run build` succeeds (tsc + vite build → `dist/`). `npm run dev` runs Vite + Electron together via `concurrently`/`wait-on`.
- **Follow-ups**: Could not visually confirm the window renders — this sandbox is missing system libs (GTK/NSS/X11) Electron's Chromium needs, and installing them needs `sudo apt-get`, which `CLAUDE.md` forbids. User opted to skip sandbox GUI verification and confirm `npm run dev` on their own machine instead. `playwright-core` was added as a devDependency for the attempted verification and kept per user request even though unused for now.
