# 4. BoardSwitcherUI

**Goal**: List boards, create/rename/delete a board, and switch the active board.
**Est.**: 1.5h
**Dependencies**: 03

## Steps
- [x] Build a board list view (sidebar or top-level screen)
- [x] Wire "create board" action to storage
- [x] Wire rename/delete actions
- [x] Wire "switch board" to set the active board in app state
- [x] Persist the active board selection across restarts

## Definition of Done
- [x] Can create, rename, delete, and switch between multiple boards — `BoardSwitcher` sidebar wired to `window.taskApi.boards`
- [x] Active board selection persists after app restart — active board id stored in `localStorage` (`taskapp:activeBoardId`), re-validated against the live board list on load

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: `src/state/useBoards.ts` hook manages board list + active board + CRUD against `window.taskApi`. `src/components/BoardSwitcher.tsx` renders the sidebar (click to switch, double-click to rename inline, × to delete with a confirm prompt, form to add). Wired into `src/App.tsx` with a placeholder content pane showing the active board's name (kanban columns/cards land in Intent 05/06). `npm run build` succeeds.
- **Follow-ups**: Same sandbox limitation as Intent 02 — couldn't visually click through the UI here (no GUI libs); needs a manual check on your machine: create a few boards, switch between them, rename one, delete one, restart the app and confirm the same board is still active.
