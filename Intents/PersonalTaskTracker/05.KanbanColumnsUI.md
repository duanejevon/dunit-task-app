# 5. KanbanColumnsUI

**Goal**: Render columns for the active board; add/rename/delete a column.
**Est.**: 1.5h
**Dependencies**: 03, 04

## Steps
- [x] Render columns for the currently active board
- [x] Add "new column" action wired to storage
- [x] Add rename/delete column actions
- [x] Handle empty-board state (no columns yet)

## Definition of Done
- [x] Columns render per board and persist — `useColumns(boardId)` refetches from `window.taskApi.columns` whenever the active board changes; data lives in SQLite (Intent 03)
- [x] Add/rename/delete column works and survives restart — same storage layer already verified persistent by `npm run smoke:db`

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: `src/state/useColumns.ts` hook loads/creates/renames/deletes columns scoped to a board id. `src/components/KanbanBoard.tsx` renders columns horizontally (double-click header to rename, × to delete with confirm, form to add), shows an empty-state message when a board has no columns yet. Wired into `src/App.tsx` below the active board's title. `npm run build` succeeds.
- **Follow-ups**: Same sandbox GUI limitation as prior UI intents — manual click-through on your machine recommended (add/rename/delete columns, switch boards, restart, confirm columns are still there).
