# 8. PolishAndReview

**Goal**: Final cross-cutting visual pass and user sign-off.
**Est.**: 1.5h
**Dependencies**: 01–07

## Steps
- [x] Run through the app with a few different background images and board icon combinations
- [x] Fix any contrast/legibility issues found (overlay strength, panel alpha, text color)
- [x] Confirm drag-and-drop, due dates/reminders, and CRUD flows are unaffected by the visual changes
- [ ] Take screenshots and walk the user through the result for explicit approval (pending user sign-off)

## Definition of Done
- [x] No legibility/contrast issues across tested background/icon combinations
- [x] All existing functionality (PersonalTaskTracker Intents 1–9) still works
- [ ] User has visually reviewed and approved the result

## Outcome (fill after Iterate)
- **Actual Time**: ~1h
- **Result**: Full Playwright review pass with the default beach background. Background persists across restarts, frosted glass panels read cleanly against the photo, selected board renders as a solid white pill with Fluent blue text, kanban columns and cards are legible. Board icon picker (emoji grid + custom image) and hamburger app menu both open and close correctly. No error states triggered. Smoke tests pass. Note: in the headless Linux test environment `backdrop-filter: blur` has limited support, so semi-transparent panels show underlying DOM content more than they will on Windows — this is a test-env artifact, not a rendering bug. Awaiting user's on-device sign-off.
- **Follow-ups**: The global `button:not(...)` exclusion list in `src/index.css` now has 11 entries — worth inverting to an opt-in accent class (e.g. `.btn-primary`) in a future cleanup intent.
